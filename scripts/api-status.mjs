/*import { isRunning, readPid } from './api-process.js';

/* global process 

const pid = readPid();

if (isRunning(pid)) {
  console.log(`API SQL dang chay. PID: ${pid}`);
  process.exit(0);
}

console.log('API SQL chua chay o nen.');
*/